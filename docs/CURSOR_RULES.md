# Rules

Never call external service inside Controller.

Never write GraphQL inside Service.

Never duplicate GraphQL Query.

Every Provider has single responsibility.

Use async/await.

Prefer reusable function.

Controller should be thin.

Business Logic only in Service.

GraphQL only inside graphql folder.
